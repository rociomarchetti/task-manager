[build]
  command = "npm ci && nx build task-manager --configuration=production"
  publish = "dist/apps/task-manager/browser"

[build.environment]
  NODE_VERSION = "20.19.0"
  NX_DAEMON = "false"
  NX_NO_CLOUD = "true"

[[plugins]]
  package = "@netlify/angular-runtime"

  [plugins.inputs]
    packagePath = "apps/task-manager"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200