[build]
  command = "npm ci && nx build task-manager --configuration=production"
  publish = "dist/task-manager"

[build.environment]
  NODE_VERSION = "20.19.0"
  NX_DAEMON = "false"
  NX_NO_CLOUD = "true"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200